# E2E tests for DCX test task with Webdriverio

## Getting started
Clone repo from:
``git clone ``

Go to ``DCX-wdio-project`` directory and:

 `nvm use 18`
 `npm install`
 
## Start the test
- ``npx wdio run wdio.conf.js ``
